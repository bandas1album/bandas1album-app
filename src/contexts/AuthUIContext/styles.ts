import styled from 'styled-components'

export const AuthWrapper = styled.div`
  display: flex;
  height: 100%;
  width: 100%;
  overflow: hidden;

  main {
    flex: 1;
    overflow-y: auto;
    display: flex;
    flex-direction: column;
  }
`
